<section class="hero">
  <div class="container">
    <div class="hero-content fade-in">
      <h1 class="hero-title stagger-1">
        Studio créatif
        <span class="highlight">Madara</span>
      </h1>
      <p class="hero-subtitle stagger-2">
        Spécialisé en branding et vidéo, dirigé par Maxime Hoffmann.<br>
        Nous créons des identités visuelles marquantes et des contenus vidéo percutants.
      </p>
      <div class="hero-cta stagger-3">
        <a href="/contact" class="btn btn-primary">Démarrer un projet</a>
        <a href="/portfolio" class="btn btn-secondary">Voir le portfolio</a>
      </div>
    </div>
    
    <div class="hero-visual fade-in stagger-4">
      <div class="visual-element"></div>
      <div class="visual-element"></div>
      <div class="visual-element"></div>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    position: relative;
    background: var(--gradient-hero);
    overflow: hidden;
  }

  .hero::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(circle at 30% 20%, rgba(0, 13, 255, 0.1) 0%, transparent 50%);
    pointer-events: none;
  }

  .container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 64px;
    align-items: center;
    position: relative;
    z-index: 1;
  }

  .hero-content {
    max-width: 600px;
  }

  .hero-title {
    margin-bottom: 24px;
    color: var(--color-dark);
  }

  .highlight {
    background: var(--gradient-primary);
    -webkit-text-fill-color: transparent;
    background-clip: text;
    display: inline-block;
  }

  .hero-subtitle {
    font-size: 20px;
    margin-bottom: 32px;
    color: var(--color-gray);
    line-height: 1.5;
  }

  .hero-cta {
    display: flex;
    gap: 16px;
    flex-wrap: wrap;
  }

  .hero-visual {
    display: flex;
    justify-content: center;
    align-items: center;
    position: relative;
    height: 500px;
  }

  .visual-element {
    position: absolute;
    border-radius: 50%;
    opacity: 0.8;
    animation: float 6s ease-in-out infinite;
  }

  .visual-element:nth-child(1) {
    width: 200px;
    height: 200px;
    background: var(--gradient-primary);
    top: 20%;
    left: 20%;
    animation-delay: 0s;
  }

  .visual-element:nth-child(2) {
    width: 120px;
    height: 120px;
    background: var(--gradient-secondary);
    top: 60%;
    right: 30%;
    animation-delay: 2s;
  }

  .visual-element:nth-child(3) {
    width: 80px;
    height: 80px;
    background: linear-gradient(135deg, var(--color-pastel-pink), var(--color-pastel-green));
    top: 10%;
    right: 10%;
    animation-delay: 4s;
  }

  @keyframes float {
    0%, 100% {
      transform: translateY(0px) rotate(0deg);
    }
    33% {
      transform: translateY(-20px) rotate(120deg);
    }
    66% {
      transform: translateY(10px) rotate(240deg);
    }
  }

  @media (max-width: 768px) {
    .hero {
      min-height: 90vh;
      padding-top: 80px;
    }

    .container {
      grid-template-columns: 1fr;
      gap: 40px;
      text-align: center;
    }

    .hero-visual {
      height: 300px;
      order: -1;
    }

    .hero-subtitle {
      font-size: 18px;
    }

    .hero-cta {
      justify-content: center;
    }

    .visual-element:nth-child(1) {
      width: 150px;
      height: 150px;
    }

    .visual-element:nth-child(2) {
      width: 90px;
      height: 90px;
    }

    .visual-element:nth-child(3) {
      width: 60px;
      height: 60px;
    }
  }
</style>